{% extends '@App/layout.html.twig' %}

{% block body %}
    <h1>Noter_crit creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('noter_crit_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
