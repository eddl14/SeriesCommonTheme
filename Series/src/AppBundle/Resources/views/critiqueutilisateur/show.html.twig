{% extends '@App/default/index.html.twig' %}

{% block main %}
    <h1>CritiqueUtilisateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ critiqueUtilisateur.id }}</td>
            </tr>
            <tr>
                <th>Libellecritique</th>
                <td>{{ critiqueUtilisateur.libelleCritique }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('critiqueutilisateur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('critiqueutilisateur_edit', { 'id': critiqueUtilisateur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
