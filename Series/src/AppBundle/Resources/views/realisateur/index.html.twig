{% extends '@App/layout.html.twig' %}

{% block body %}
      {% block label %}
        <h1> Liste des Réalisateurs</h1>
    {% endblock %}
    
  <a class="btn icon-btn btn-success" href="{{ path('realisateur_new') }}">Ajouter
          <span class="glyphicon btn-glyphicon glyphicon-plus img-circle text-success"></span>
      </a>
   
        <tbody>
            <ul class="list">
                {% for realisateur in realisateurs %}
                    <li>
                        
                        <a href="{{ path('realisateur_show', { 'id': realisateur.id }) }}" class="framelist" >              
                            {% if realisateur.Photo !=null %}
                                 <img src="{{realisateur.Photo}}" />
                              {% else %}
                                  <img src="{{asset('Img/avatar.gif')}}" />
                            {% endif %} 
                           
                            <div class="bg">                                
                                    {{ realisateur.prenomRealisateur }}
                                    {{ realisateur.nomRealisateur }}                                    
                            </div>             
                            
                              
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </tbody>
    </table>

{% endblock %}
