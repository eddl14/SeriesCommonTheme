{% extends '@App/default/index.html.twig' %}

{% block main %}
    <h1>Noter_ser creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('noter_ser_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
