{% extends 'base.html.twig' %}

{% block body %}
    <h1>Noter_ser</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ noter_ser.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ noter_ser.note }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('noter_ser_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('noter_ser_edit', { 'id': noter_ser.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
